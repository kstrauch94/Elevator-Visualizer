%Elevator Instance

#program base.

#const floors = 10.

#const start1 = 3.
#const start2 = 5.
#const start3 = 3.
#const start4 = 5.


#const startingReqs = 2.

#const agents=4.    % number of elevators
agent(elevator(1..agents)).

floor(1..floors).

init(at(elevator(1), 3)).
init(at(elevator(2), 5)).

init(at(elevator(3), 3)).
init(at(elevator(4), 5)).

init(request(deliver(1), 2)).
init(request(call(up), 9)).


